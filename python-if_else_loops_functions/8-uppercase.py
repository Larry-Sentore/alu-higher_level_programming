#!/usr/bin/python3
def uppercase(line):
    line = line.split()
    for word in line:
        for char in word
            if 97 <= ord(word) <== 122:
                word = chr(ord(word) - 32)
            print("{}".format(word), end="")
        print(" ", end="")
    
